# LifeOS — Master Brief

> Single-source content pack for feeding LLMs to generate marketing copy, landing pages, pitch decks, social posts, and press kits. Distilled from the full product + technical docs in this folder. Self-contained — paste any section (or the whole file) into another LLM as context.

---

## 1. Positioning

- **Name:** LifeOS
- **One-liner:** The AI-first Digital Life Architect — answers *"What should I do next to improve my life?"* every single day.
- **Category:** AI-native personal life management (not a to-do app, not a habit tracker).
- **Tagline options:**
  - "Your life, synthesised."
  - "Six engines. One life. One plan."
  - "Stop juggling apps. Start living a routine."
- **Elevator pitch (30s):** People juggle goals, health, money, career, relationships, and curiosity across a dozen disconnected apps. LifeOS is the first mobile app that uses AI to understand all six dimensions of your life, then synthesises them into a single livable daily routine — updated continuously, gamified for momentum, and entirely private-by-design (your data never leaves your device).
- **Elevator pitch (2-min):** LifeOS is a React Native app powered by Claude AI that acts as a Digital Life Architect. Onboarding captures your vision, career targets, and schedule — or imports an existing ChatGPT/Claude profile via the Discovery flow — then uses AI to decompose your life goals into a hierarchy (yearly → monthly → weekly → daily), analyse your skill gaps, and generate a full daily routine. It keeps pulling from six engines — Goals, Health, Finance, Career, Social, and Polymath (curiosity/learning) — to continuously answer the one question that matters: *what should I do next?* A built-in gamification layer (XP, levels, streaks, badges, a hexagonal Life Balance radar) makes momentum visible and the app feels alive. Finance auto-ingests bank and UPI transactions from Gmail, categorises them with AI, and surfaces behavioural insights. Google Calendar and Google Fit are wired in; an Ask LifeOS chatbot and voice assistant answer questions over your own data; an evening Reflect ritual closes the loop. Sensitive data stays on-device (SQLite native, localStorage/IndexedDB on web); AI inference runs through a Cloudflare Worker proxy with admin-managed prompts and feature flags, and Supabase handles optional Google sign-in.

---

## 2. The Problem

- Modern ambitious people run their life across 8–15 apps: Notes, Calendar, Todoist, MyFitnessPal, YNAB, LinkedIn Learning, Strava, Duolingo, journaling apps, etc.
- None of them talk to each other. None of them answer the *actual* question: *given everything going on in my life, what should I do next?*
- Productivity apps optimise tasks. Habit apps optimise repetition. Neither optimises **your life as a whole system.**
- Health, finance, and relationships are deeply personal — most existing tools require you to hand this data to a cloud provider.

## 3. The Solution

LifeOS synthesises your whole life into one daily plan. Six specialised AI engines feed one master planner:

| # | Engine | What it does |
|---|--------|--------------|
| 1 | **Goal Intelligence** | Decomposes a life vision into a yearly → monthly → weekly → daily hierarchy with AI |
| 2 | **Health Intelligence** | Calorie ring, macro tracking, AI photo food recognition, blood report parsing + suggestions |
| 3 | **Financial Goal Engine** | AI-generated savings/investment plans; auto-ingests bank transactions from Gmail (HDFC/ICICI/Axis) and categorises with Claude |
| 4 | **Career & Upskill** | Skill-gap analysis between current and target role; curated learning resources with progress tracking; named career-path snapshots |
| 5 | **Social Intelligence** | Contact cadence + relationship depth (WIP) |
| 6 | **Curiosity / Polymath** | Interests, weekly learning targets, exploration log — feeds the "mind" domain score |

All six feed the **Routine Builder** — a time-blocked daily schedule generated by AI from your wake/sleep/work hours and your active goals.

## 4. Target User & Use Cases

- **Primary persona:** Ambitious 25–40-year-old professional optimising multiple life domains simultaneously — not just work.
- **Secondary persona:** Career switchers, first-time parents, founders, grad students, anyone rebuilding a life stage.
- **Use cases:**
  - "I want to lose 10 kg, get promoted, save for a house, and learn piano — show me what today looks like."
  - "I got a blood test back. What should I actually change?"
  - "Where did my salary go this month, and what's the one habit costing me the most?"
  - "I want to move from Data Analyst to ML Engineer in 12 months. What am I missing?"

## 5. Differentiators (vs. Notion, Todoist, Reclaim, Finch, Habitica, Headspace)

1. **Whole-life synthesis, not single-domain optimisation.** The only app that plans across goals + health + finance + career + social + curiosity in one routine.
2. **AI-native from day one.** Claude Sonnet is wired into every meaningful decision: goal decomposition, routine generation, skill-gap analysis, food photo recognition, blood report parsing, transaction categorisation, weekly insights.
3. **Private by design.** Sensitive data (health logs, food entries, blood reports, on-device chat) lives on-device only — SQLite on native, IndexedDB / localStorage on web. A Cloudflare Worker proxies AI calls (so the Anthropic key never ships in the bundle) and a Supabase project handles auth + opt-in anonymous telemetry. Telemetry is OFF by default and uses a random device id with no PII; users can disable it and export or delete all data from Settings.
4. **Bold, expressive, alive.** Duolingo × Headspace aesthetic. Hexagonal Life Balance radar, animated level rings, XP chips, streak flames — gamification that feels premium, not toy-like.
5. **Works on iOS, Android, and Web** from the same codebase, with automatic storage swapping (SQLite native, localStorage / IndexedDB on web). The web build ships as an installable PWA on Cloudflare Pages.
6. **Finance intelligence from your inbox.** Gmail OAuth + regex parsing (bank + UPI) + AI categorisation means transaction tracking without Plaid or bank logins.
7. **Admin-controlled AI.** A Cloudflare Worker `ai-proxy` plus an admin portal (feature flags + prompt registry) means prompts, models, and rollout can be tuned live without a client release.

## 6. Feature Highlights (marketing-ready bullets)

### Onboarding — 3 screens to a full routine
- Enter your vision → AI decomposes into yearly/monthly/weekly/daily goals.
- Enter your current + target role → AI finds your skill gaps and suggests learning resources.
- Enter your wake/sleep/work hours → AI generates 24 time-blocked routine blocks for today.
- **Discovery Import** — paste a ChatGPT or Claude conversation and AI extracts a starter profile (vision, role, routine) to seed LifeOS in one step.
- Welcome-intent screen + a How-it-works walkthrough and Terms/Privacy screens for first-run orientation.

### Today Screen
- Avatar with Level Ring showing XP progress in current level
- Hexagonal **Life Balance radar** — one glance shows the health of all six life domains
- Live streak + XP chips
- AI-generated **Daily Briefing** and **Weekly Insight** from your behaviour analytics
- Time-sorted routine blocks — tap to complete, haptic feedback, instant XP reward
- Evening **Reflect** ritual — close the day, log wins, and get an AI-suggested tweak for tomorrow

### Ask LifeOS & Voice
- **Ask LifeOS chatbot** — read-only Q&A over your own profile, goals, routine, finance, and health data, available from the Profile sidebar
- **Voice assistant** — hands-free interaction via Gemini Live API (native audio model `gemini-2.5-flash-native-audio-preview-12-2025`). Web mic capture (PCM16 16 kHz) with real-time waveform visualization and server-side VAD turn detection. Proxied through the Cloudflare Worker with in-band JWT auth.

### Health
- Calorie ring with protein / carb / fat / fibre macro breakdown vs. 2000-kcal target
- **Photo food logging** — snap a meal, AI identifies items and macros
- 7-day weight trend chart
- **Blood report upload** — AI parses markers, summarises, and gives action items

### Finance
- AI-generated financial plan tailored to goal + income + savings + risk profile, denominated in INR
- **Gmail integration** — auto-ingests bank transaction emails (HDFC, ICICI, Axis) and **UPI** notifications via OAuth
- Rule-based + AI merchant categorisation
- Weekly AI insights: spending spikes, new merchants, category drift
- Milestone tracking toward the goal

### Career
- Visual skill-gap chart (current vs. required level)
- Curated learning resources with status (not_started / in_progress / completed)
- **Save Path** — name and persist career-path snapshots; load or delete later
- **Elite Career Strategist** — long-form AI strategy generation alongside the skill-gap view

### Integrations
- **Google Calendar** — read + write events into the routine
- **Google Fit** — pull activity, weight, and vitals
- **Gmail** — finance ingest (bank emails + UPI)
- **Google sign-in (Supabase)** — optional cloud auth on web; local email/password auth still supported

### Rewards Tab (Gamification)
- **XP + Levels** — triangular curve, L1→L10 ≈ 5,500 XP
- **5 streaks** — workout, learning, food tracking, journaling, social — with 1-day grace
- **8 badges** — first_blueprint, first_blood_report, streak_30_any, skill_mastery, life_balance, goal_complete, week_1, food_photo
- **Daily + weekly quests** with module-colour progress chips
- **Full-screen level-up overlay** + achievement toasts

### Polymath / Explore
- **Discover grid** — Instagram-style 3-column masonry of curated areas (Astronomy, Jazz piano, Photography, Stoicism, Climbing, Spanish, AI & ML, etc.) with one 2×2 feature tile per block. Tap a tile to open the Add Interest sheet pre-filled with the suggestion's name + category.
- Interests CRUD, weekly target tracking, exploration log with minute chips
- Feeds XP, the learning streak, and the "mind" domain score

### Notifications
- Onboarding nudges at day 3, 7, 14
- Daily routine reminder at wake time
- Goal reminder at 3 PM, streak-at-risk at 8 PM
- Weight reminder 21 days after last log

---

### Information Architecture
- **5-tab bottom nav:** Today, Life (hub linking Goals / Health / Finance / Career / Polymath), Rewards, Explore, Profile.
- Profile screen surfaces **usage analytics** and acts as the entry point to chat, settings, integrations, and how-it-works.

## 7. Design DNA

- **Vibe:** Duolingo × Headspace × Finch × Habitica HUD — adult and premium, never childish. Current visual layer is the **Aurora Glass** redesign — soft glass surfaces, gradient blooms, and motion-driven depth.
- **Typography:** Nunito (display/headings, 700/800) + DM Sans (body/labels, 400/500/600). Scale 11 / 13 / 15 / 17 / 20 / 28 / 36 / 48.
- **Grid:** 4pt. Cards radius 20, buttons 16, pills 999.
- **Motion:** Reanimated-driven micro-animations on every state change. Haptics on meaningful taps.
- **Module colours (each engine has an identity):**
  - Goal `#FF6B35` orange · Health `#00C896` green · Finance `#F0B429` gold
  - Career `#5B4FE8` violet · Social `#FF4D8B` pink · Polymath `#00B4D8` cyan
- **Gamification colours:** XP `#FFD700` gold · Streak `#FF6B35` flame · Badge `#A855F7` purple
- **Brand:** primary `#5B4FE8` deep violet
- **Dark mode default** — `#0D0D0D` bg, `#1A1A2E` surface, `#1F1F3A` card. **Reactive light mode** — `useColors()` hook + `makeStyles(c: AppColors)` pattern across the consumer app; Settings → Appearance toggle drives a live re-render.

## 8. Tech Stack (for technical marketing / dev-facing content)

| Layer | Tech |
|-------|------|
| Runtime | React Native 0.81.5 + Expo 54 |
| Language | TypeScript 5.9 strict |
| Routing | Expo Router 6 (file-based) |
| State | Zustand 5 |
| DB (native) | SQLite via expo-sqlite 16 + Drizzle ORM |
| DB (web) | localStorage + IndexedDB (Dexie) for finance transactions |
| AI | Claude Sonnet 4 (Anthropic REST API) |
| Async | TanStack React Query 5 |
| Animation | Reanimated 4 + react-native-svg |
| Validation | Zod |
| Auth | Local email/password (expo-crypto SHA-256 + per-user salt) **and** Google sign-in via Supabase on web; admin portal uses Supabase magic-link (default) with password fallback |
| Backend | Cloudflare Worker `ai-proxy` v4 (model routing, prompts, flags, telemetry, feedback, push, evals, OAuth token exchange) + Supabase (auth + admin tables only) — no consumer app data persisted server-side |
| Admin | Next.js 14 admin portal (Vercel) with Flags · Prompts · Telemetry · Schema failures · Evals · Feedback · Push tabs. Audit-logged on every mutation. Live admin URL: project-yom9m.vercel.app |
| AI infra | Cost ledger · model router · tracing · **agentic Routine Planner** (retrieve → propose → critique → commit) wired into both consumer entry points · **RAG** seeded from recent reflections + behaviour events + blood-report summaries via `src/ai/historyContext.ts` · **eval harness** at `evals/` runs in CI on every PR touching `src/ai/**` (7 suites, 28+ graders) and writes pass rates back to the admin |
| Finance | 4-tier categorizer: cache → rule → **distilled local k-NN classifier** (char-3-gram TF-IDF, zero deps) → batched AI fallback. The classifier holds 167 anchors and serves most rule-misses for free; AI escalations are batched at 25/req so a 100-merchant sync uses ≤2 AI calls |
| Telemetry | Anonymous opt-in (default OFF). Anonymous device id, server-side event allowlist, 4 KB props cap, per-device daily cap. Funnel + recent-events dashboards in admin |
| Web deploy | Cloudflare Pages PWA (manifest, iOS standalone meta, static export). Live web URL: lifeos-6r5.pages.dev |
| Platforms | iOS, Android, Web (same codebase) |

- **Sensitive data is on-device.** SQLite native / localStorage + IndexedDB on web. Cloudflare Worker proxies AI inference; Supabase handles auth only.
- **Storage abstraction:** every query branches on `Platform.OS === 'web'` to swap SQLite ↔ localStorage/Dexie — transparent to callers.
- **Mock mode** (`EXPO_PUBLIC_USE_AI_MOCK=true`) lets the full app run with zero API key during dev.

### AI functions
Single-shot: `decomposeGoal`, `analyseSkillGap`, `parseBloodReport`, `suggestMeals`, `generateFinancialPlan`, `getWeeklyFinanceInsight`, `categorizeMerchant` + **batched** `categorizeMerchantsBatch`, `recogniseFood`, `generateCareerStrategy`, `describeGoal`, `generateMotivation`, `extractDiscoveryProfile`, `suggestTomorrowTweak`, `discoveryChatTurn`.

Multi-step: `planRoutineAgent` (the agentic Routine Builder) wraps `generateRoutine` in a `retrieve → propose → critique → commit` loop with traced intermediate state. The public entry point `planRoutineWithContext` (`src/ai/routinePlanner.ts`) builds the RAG context from recent user history before invoking the agent.

Every function is Zod-validated. Validation failures emit an `ai_schema_failure` telemetry event with the failing task + schema + raw response, which surfaces in the admin's **Schema failures** tab so production failures become eval-fixture candidates.

## 9. Privacy & Trust Story

- Health logs, blood reports, contacts, transactions — all stored in on-device SQLite (or localStorage/IndexedDB on web).
- AI inference is proxied through a Cloudflare Worker so API keys never ship to the client; the Worker does not persist user content.
- Supabase is used for optional Google sign-in only; LifeOS app data is not synced to Supabase in the current phase.
- Soft deletes on all user data (`deletedAt` column) — deletion is always recoverable client-side before purge.

## 10. Roadmap / Phasing

- **Phase 1 (shipped):** Onboarding (incl. Discovery Import), Goals, Health, Finance (+ Gmail/UPI ingest, INR), Career (incl. Elite Career Strategist + Save Path), Polymath, full gamification overhaul (Rewards tab, levels, hex radar, quests, badges). Google Calendar + Google Fit integrations. Ask LifeOS chatbot + voice assistant. Aurora Glass redesign. PWA on Cloudflare Pages with Supabase Google sign-in. Cloudflare Worker `ai-proxy` + admin portal (flags + prompt registry).
- **Phase 2 (shipped):** Finance Engine depth (full plan generator + weekly insights). Photo Food Recognition. Push notification depth (goal / streak / social nudges). Behaviour Intelligence v1 (observe + weekly insight).
- **Phase 3 (shipped):** Social Life Intelligence Engine with on-device contacts and AI-personalised conversation starters that never see names. Curiosity & Polymath Engine with AI-driven Discover grid + cross-discipline link card + protected-time toggle. Routine Builder v2 with weekday/weekend differentiation, energy-required matching, adaptive rebalance signal + multi-day generation, and a "Plan my next 7 days" entry point. Behaviour Intelligence v2 (Adapt) with approve/dismiss adaptation cards and a 28-day monthly insight report. Day 3 / 7 / 14 progressive onboarding screens. (See `TASKS.md` P3-01 through P3-05.)
- **Phase 4 (shipped):** Cross-Module Life Score with 30/90-day trend hero. Proactive Daily Briefing (AI morning banner, cached daily). Annual Life Review (cross-domain AI report, PDF/share export, cached yearly). Long-Term Trajectory Tracking ("on track for your 3-year vision?" with quarterly recalibration). (See `TASKS.md` P4-01 through P4-04.)
- **Future:** Plaid integration for broader finance coverage; native HealthKit / Health Connect integration; optional cloud sync (Supabase) of app data.

## 11. Metrics / Proof Points for Decks

- **6 life engines** feeding 1 master planner
- **23 AI functions**, all Zod-validated, all with mock fallbacks (added in Phase 3/4+: `generateConversationStarters`, `suggestInterestAreas`, `suggestCrossDisciplineLink`, `generateWeekRoutine`, `generateMonthlyInsightReport`, `generateDailyBriefing`, `assessTrajectory`, `generateAnnualReview`, `generateMoneyReview`)
- **11 badges, 5 streaks, 12+ level tiers, 2–3 daily + 1 weekly quest**
- **3-screen onboarding** (or 1-step Discovery Import) **→ full AI-generated routine**
- **3 banks + UPI** supported for Gmail-based transaction ingest (HDFC, ICICI, Axis) — extensible via regex
- **iOS, Android, and Web (PWA on Cloudflare Pages)** from a single codebase
- **Cloudflare Worker AI proxy + admin portal** for live prompt/flag control

## 12. Sample Taglines, Headlines & Hooks

**Taglines**
- Your life, synthesised.
- What should I do next? Ask LifeOS.
- Six engines. One life. One plan.
- Stop juggling apps. Start living a routine.
- The Digital Life Architect in your pocket.

**Landing-page headlines**
- "Your whole life — planned by AI, every single day."
- "One app. Six life engines. Zero guesswork."
- "The first AI that plans your *life*, not just your tasks."
- "From vision to Monday morning — in three screens."

**Social hooks**
- "I stopped using 11 apps this month. Here's the one that replaced them."
- "This app turned a one-sentence life vision into my Monday schedule."
- "An AI parsed my blood test and told me exactly what to change."

**Press / launch angles**
- "LifeOS brings agentic AI to personal life management — entirely on-device."
- "A Digital Life Architect that reads your Gmail, your blood test, and your calendar — and never uploads any of it."

## 13. FAQs for FAQ sections / sales enablement

- **Is my data private?** Yes — sensitive data (health, finance, contacts) stays in on-device storage. AI inference goes through a Cloudflare Worker proxy that does not persist user content. Supabase is used only for optional Google sign-in.
- **Do I need an API key?** No — LifeOS ships with a mock mode and routes real AI calls through the Worker.
- **Does it work offline?** Yes for everything except live AI calls.
- **Which banks are supported for Finance?** HDFC, ICICI, Axis, and UPI today (Gmail-based, regex-parsed). New banks = new parser.
- **Is there a web version?** Yes — the same codebase runs on iOS, Android, and Web (installable PWA on Cloudflare Pages). Web uses localStorage + IndexedDB instead of SQLite.
- **What AI model powers it?** Claude Sonnet 4 via the Anthropic REST API, routed through the `ai-proxy` Worker with admin-managed prompts and feature flags.

## 14. Boilerplate (short / medium / long)

**Short (50 words)**
LifeOS is the AI-first Digital Life Architect for iOS, Android, and Web. Six engines — Goals, Health, Finance, Career, Social, and Curiosity — feed one master planner that tells you what to do next. Gamified, private by design, powered by Claude.

**Medium (100 words)**
LifeOS answers one question continuously: *what should I do next to improve my life?* It's an AI-first mobile app that synthesises six life dimensions — Goals, Health, Finance, Career, Social, and Curiosity — into a single livable daily routine. Powered by Claude, it decomposes your vision into a goal hierarchy, analyses your skill gaps, auto-ingests transactions from Gmail, parses blood reports, and generates time-blocked days. A hexagonal Life Balance radar, XP, streaks, levels, and badges keep momentum visible. Built on React Native + Expo, runs on iOS, Android, and Web, with all personal data stored on-device.

**Long (200 words)**
LifeOS is an AI-first mobile app — a Digital Life Architect — that answers the one question that matters every morning: *what should I do next to improve my life?* Unlike productivity apps that optimise tasks or habit apps that optimise repetition, LifeOS optimises your life as a whole system. Six specialised AI engines — Goal Intelligence, Health Intelligence, Financial Goal, Career & Upskill, Social, and Polymath (curiosity/learning) — feed a single Master Routine Builder that generates a time-blocked day tailored to your vision, your energy, and your schedule. Claude Sonnet powers nine AI functions: goal decomposition, skill-gap analysis, routine generation, food photo recognition, blood report parsing, financial plan generation, weekly insights, Gmail-sourced transaction categorisation, and more. A bold, expressive gamification layer — levels, XP, five streak types, eight badges, daily and weekly quests, and a hexagonal Life Balance radar — makes momentum visible. Built on React Native + Expo, TypeScript strict, Zustand, Drizzle ORM, and Zod, it runs on iOS, Android, and Web from a single codebase. Sensitive data lives on-device; a Cloudflare Worker proxies AI calls and a Supabase project handles auth + opt-in anonymous telemetry. Private by design, AI-native by default, alive by aesthetic.

---

*Source docs: `PRODUCT_TECHNICAL_DOC.md`, `AI_FUNCTIONS.md`, `FINANCE_PIPELINE.md`, `GAMIFICATION.md`, `WEB_VS_NATIVE.md`, `claude-design-prompt-mobile.md`, `claude-design-prompt-web.md`. Keep this file in sync when those change.*
